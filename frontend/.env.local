# API base URL — used by src/api/client.ts
# VITE_ prefix required for Vite to expose this to the browser bundle
# .env.local is git-ignored by default — safe for local secrets
VITE_API_BASE=http://localhost:8000
VITE_WS_BASE=ws://localhost:8000